name: Generate Cards

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  generate-cards:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=NullPointerDepressiveDisorder&show_icons=true&count_private=true&theme=tokyonight&hide_border=true&bg_color=0D1117
          path: stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate top languages card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: top-langs
          options: username=NullPointerDepressiveDisorder&layout=compact&theme=tokyonight&hide_border=true&bg_color=0D1117
          path: top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and Push
        uses: EndBug/add-and-commit@v9
        with:
            add: '["stats.svg", "top-langs.svg"]'
            message: "Update GitHub Profile Cards"
